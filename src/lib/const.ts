export const RESEND_COOLDOWN_SECONDS = 60;       // min gap between two sends to the same number
export const MAX_PER_MOBILE_PER_WINDOW = 3;      // max sends per number per window
export const MOBILE_WINDOW_MINUTES = 10;
export const MAX_PER_IP_PER_WINDOW = 10;         // max sends per IP per window (covers rotating numbers)
export const IP_WINDOW_MINUTES = 60;